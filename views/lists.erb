<ul id="lists"> 
	<% @lists.each_with_index do |list, index| %>
    <% if list[:todos].all? { |td| td[:completed] } %>
      <li class="complete">
    <% else %>
      <li>
    <% end %>
      <a href="/lists/<%= index %>" class="complete"> 
        <h2><%= list[:name] %></h2>
        <p><%= list[:todos].size %></p>
      </a>
    </li>
  <% end %>
</ul>

<% content_for :header_links do %> 
  <a href="/lists/new" class="add">New Lists</a>
<% end %> 
